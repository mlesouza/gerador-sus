import { Layout } from 'antd';
import styled from 'styled-components';

const { Content } = Layout;

export const UploadLayout = styled.div`
  min-height: 100vh;
  background-color: #f0f2f5;
`;

export const UploadContent = styled(Content)`
  padding: 0 30px;
`;
